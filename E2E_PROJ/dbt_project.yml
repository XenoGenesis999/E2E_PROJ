name: E2E_PROJ
version: 1.0.0
config-version: 2
profile: E2E_PROJ
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  E2E_PROJ:
    +database: PROJ
    bronze:
      +schema: BRONZE
      +materialized: table

    silver:
      +schema: SILVER
      +materialized: view

    gold:
      +schema: GOLD
      +materialized: table
